---
export interface Props {
  class?: string;
  gold?: boolean;
}

const { class: className = '', gold = false } = Astro.props;
---

<div class:list={[gold ? 'gradient-border-gold' : 'gradient-border', className]}>
  <slot />
</div>
