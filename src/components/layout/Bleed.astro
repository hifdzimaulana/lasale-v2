---
export interface Props {
  class?: string;
  as?: 'div' | 'figure' | 'section';
  height?: string;
}

const { class: className = '', as: Tag = 'div', height } = Astro.props;
---

<Tag class:list={['full-bleed', className]} style={{ height }}>
  <slot />
</Tag>
