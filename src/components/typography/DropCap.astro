---
export interface Props {
  class?: string;
  light?: boolean;
}

const { class: className = '', light = false } = Astro.props;
---

<span class:list={[light ? 'drop-cap-light' : 'drop-cap', className]}>
  <slot />
</span>
